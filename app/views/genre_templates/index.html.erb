<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All genres
    </h1>

    <a href="/genres/new" class="btn btn-block btn-outline-secondary">
      Add a new genre
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Name
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @genres.each do |genre| %>
      <tr>
        <!-- Display genre.id start -->
        <td>
          <%= genre.id %>
        </td>
        <!-- Display genre.id end -->

        <!-- Display genre.name start -->
        <td>
          <%= genre.name %>
        </td>
        <!-- Display genre.name end -->

        <!-- Display genre.created_at start -->
        <td>
          <%= time_ago_in_words(genre.created_at) %> ago
        </td>
        <!-- Display genre.created_at end -->

        <!-- Display genre.updated_at start -->
        <td>
          <%= time_ago_in_words(genre.updated_at) %> ago
        </td>
        <!-- Display genre.updated_at end -->

        <td>
          <a href="/genres/<%= genre.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @genres, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
